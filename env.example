# Groq API Configuration
# Get your API key from: https://console.groq.com/keys
NEXT_PUBLIC_GROQ_API_KEY=your_groq_api_key_here

# Note: In production, it's recommended to use server-side API routes
# instead of exposing the API key in the browser. This is for demo purposes.
